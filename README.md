# 🛒 E-commerce Backend System

Даний проєкт — бекенд-застосунок для електронної комерції, реалізований у рамках навчального курсу з баз даних та бекенд-розробки.
Система моделює основні бізнес-процеси інтернет-магазину: роботу з клієнтами, товарами, категоріями, замовленнями, платежами та доставкою.

Основна увага приділяється:

- коректному проєктуванню реляційної бази даних;
- реалізації бізнес-логіки;
- тестуванню бізнес-правил із використанням тестової бази даних;
- контейнеризованому запуску додатку.

---

## 🛠 Технологічний стек

### Мова програмування

- **Node.js** (JavaScript, ES2022)

### Бекенд

- **Express.js** — HTTP-сервер
- **Prisma ORM** — робота з базою даних
- **PostgreSQL** — реляційна СУБД

### Тестування

- **Jest** — фреймворк для тестування

### Контейнеризація

- **Docker**
- **Docker Compose**

---

## ⚙️ Інструкції з налаштування та запуску

### 1. Клонування репозиторію

```bash
git clone https://github.com/nnaaastyyyaaa/bd-course-coursework.git
cd bd-course-coursework
```

---

### 2. Налаштування змінних середовища

Створіть файл `.env` у корені проєкту:

```env
DATABASE_URL=postgresql://postgres:postgres@db:5432/ecommerce?schema=public
```

Для тестів використовується окремий файл `.env.test`:

```env
DATABASE_URL=postgresql://postgres:postgres@db:5432/ecommerce_test?schema=public
```

---

### 3. Запуск додатку та бази даних

```bash
docker-compose up --build
```

Ця команда:

- збирає Docker-образ бекенд-застосунку;
- запускає PostgreSQL;
- застосовує міграції Prisma;
- запускає HTTP-сервер.

Сервер буде доступний за адресою:

```
http://localhost:3000
```

---

## Запуск тестів

Тести виконуються з використанням **окремої тестової бази даних**, яка очищується перед запуском кожного тестового набору.

### Запустити всі тести

```bash
npm test
```

### Запустити конкретний тестовий файл

```bash
npx jest tests/file.test.js
```

---

## Бізнес-логіка (основні правила)

- Кожен клієнт може мати **декілька замовлень**.
- Замовлення може містити **кілька товарів різних категорій**.
- Товар може належати **одній або кільком категоріям**.
- Кожне замовлення має **адресу доставки**.
- Клієнт може мати **декілька адрес доставки**.
- Кожне замовлення повʼязане **з одним платежем та однією доставкою**.

Ці правила перевіряються за допомогою автоматичних тестів.

---

## Приклад використання API

### Створення замовлення

```http
POST /orders
Content-Type: application/json
```

```json
{
  "client_id": 2,
  "address": {
    "country": "Ukraine",
    "city": "Lviv",
    "street": "Lvivska, 324B",
    "postal_code": "09845"
  },
  "worker_id": 3,
  "items": [
    {
      "quantity": 25,
      "product_id": 2
    }
  ],
  "carrier": "УКРПОШТА",
  "payment_method": "by_card"
}
```

---

## Архітектура Docker

Проєкт складається з двох основних контейнерів:

- **db** — PostgreSQL
- **app** — Node.js + Express + Prisma

Взаємодія між сервісами відбувається через внутрішню Docker-мережу.

---

## Висновок

Проєкт демонструє практичне застосування:

- ORM (Prisma);
- реляційного моделювання даних;
- бізнес-логіки бекенд-застосунків;
- автоматизованого тестування;
- контейнеризації з використанням Docker Compose.
